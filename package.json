{
  "name": "midi-components",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build:webpack": "webpack --mode=production --inline --colors --progress --content-base src",
    "build": "npm run clean; npm run build:webpack; cp -r src/assets dist/assets; cp -r src/index.html dist/index.html",
    "clean": "rm -rf dist",
    "start": "webpack-dev-server --mode=development --inline --colors --progress --content-base src",
    "test": "NODE_PATH=./src/app mocha -r esm test/**/*.test.js",
    "lint": "eslint src"
  },
  "devDependencies": {
    "eslint": "^5.9.0",
    "esm": "^3.0.84",
    "mocha": "^5.2.0",
    "raw-loader": "^0.5.1",
    "webpack": "^4.25.1",
    "webpack-cli": "^3.1.2",
    "webpack-dev-server": "^3.1.10"
  },
  "dependencies": {
    "three": "^0.92.0"
  }
}
